// Which embed documents exist is data (what the event publishes), not a fixed
// list to render ahead of time; like the hosted pages, the document resolves
// in the client.
export const prerender = false;
