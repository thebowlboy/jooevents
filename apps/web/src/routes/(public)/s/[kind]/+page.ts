// The set of hosted pages is data (which surfaces the event publishes), not a
// fixed list to render ahead of time, so this route resolves in the client like
// every other addressed record in the app.
export const prerender = false;
